// Declarações iniciais
int a, b = 3, c;
int resultado;

// Atribuições
a = 10;
b *= 2;
c = a / b;

// Comando if-else
if (c == 1) {
    resultado = a + b;
} else {
    resultado = a - b;
}

// Comando switch-case
switch (resultado) {
    case VALOR1: {
        x = x + 1;
        break;
    }
    case VALOR2: case VALOR3: {
        x = x * 2;
        break;
    }
    default: {
        x = 0;
    }
}

